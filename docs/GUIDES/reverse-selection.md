# 反选批量选号功能使用指南

反选批量选号是 makeNeoRich 彩票选号助手的高级功能，帮助您快速生成多组号码，并通过"反选"方式排除不满意的号码，批量保存剩余号码。

## 目录

- [功能概述](#功能概述)
- [使用方法](#使用方法)
- [功能详解](#功能详解)
- [最佳实践](#最佳实践)
- [常见问题](#常见问题)
- [技巧和建议](#技巧和建议)

---

## 功能概述

反选批量选号提供：

- ✅ **批量生成**: 一次生成多组候选号码（可自定义组数）
- ✅ **智能反选**: 通过"反选"排除不满意的号码
- ✅ **批量保存**: 一键保存所有剩余的号码组
- ✅ **最后一组处理**: 特殊处理最后一组无法反选的情况
- ✅ **多种策略**: 支持不同的随机选号策略

### 适用场景

1. **想多买几注**: 一次生成10组号码，增加中奖机会
2. **有选择困难**: 让系统批量生成，然后筛选
3. **批量购买**: 节省时间，快速获得多组号码
4. **排除法选号**: 通过排除不想要的号码来决定

---

## 使用方法

### 步骤1: 打开反选功能

1. 在主页面，找到 **"反选批量选号"** 按钮
2. 点击打开反选弹窗

### 步骤2: 设置生成参数

```
生成组数: [5-10] 组
选择策略: [平衡奇偶 / 和值范围 / 完全随机]
```

**推荐设置**:
- 初次使用：生成 5 组
- 想要更多选择：生成 10 组
- 根据个人喜好选择策略

### 步骤3: 生成候选号码

点击 **"开始生成"** 按钮，系统会：
- 根据选择的策略生成多组号码
- 显示所有号码组
- 标注组号（1, 2, 3...）

### 步骤4: 查看和反选

生成后，您可以：
- ✅ **查看**: 点击每组号码查看详情
- ❌ **反选**: 点击"反选"按钮排除该组号码
- ⭐ **标记**: 标记喜欢的号码组

### 步骤5: 批量保存

完成筛选后：
1. 点击 **"批量保存剩余号码"** 按钮
2. 系统将保存所有未被反选的号码
3. 显示保存成功的提示

---

## 功能详解

### 1. 号码生成

#### 生成组数

| 组数 | 适用场景 | 处理时间 |
|------|---------|---------|
| 5组 | 快速选择，初次尝试 | ~1秒 |
| 8组 | 中等数量选择 | ~2秒 |
| 10组 | 大量选择，批量购买 | ~3秒 |

#### 选号策略

**平衡奇偶策略**
```
特点: 红球奇偶比例接近 3:3
适用: 追求号码平衡分布
示例: 02, 15, 18, 23, 28, 31 + 07
```

**和值范围策略**
```
特点: 红球和值在 80-120 之间
适用: 追求合理的和值范围
示例: 05, 12, 19, 23, 27, 30 + 12 (和值: 116)
```

**完全随机策略**
```
特点: 不受任何限制，完全随机
适用: 相信随机性，不设限制
示例: 完全随机生成
```

### 2. 反选操作

#### 什么是反选？

反选 = "反向选择" = "排除不想要的"

传统方式：选择想要的号码
反选方式：排除不想要的号码

**优势**:
- 有时候更容易判断"不想要什么"
- 逐步缩小范围，减少选择压力
- 批量处理，提高效率

#### 反选操作

1. **查看号码组**
   ```
   第 1 组: 02, 08, 15, 19, 26, 31 + 07
   ```

2. **判断是否满意**
   - 满意：保留，不做操作
   - 不满意：点击"反选"按钮

3. **反选后的状态**
   ```
   第 1 组: 02, 08, 15, 19, 26, 31 + 07 [已反选]
   显示: 灰色，不可再次反选
   ```

4. **取消反选**
   - 如果误操作，点击"恢复"按钮
   - 号码组重新变为可选状态

### 3. 批量保存

#### 保存逻辑

```
生成10组号码
    ↓
反选了 3 组不满意的
    ↓
剩余 7 组满意的
    ↓
批量保存这 7 组到历史记录
```

#### 保存格式

每组号码会保存为一条独立的历史记录，包含：
- 彩票类型（双色球/大乐透）
- 红球号码
- 蓝球号码
- 生成时间
- 生成策略
- 来源标记（反选批量）

#### 查看保存的号码

1. 进入"历史记录"页面
2. 查看刚保存的多组号码
3. 每组号码都有独立的"反选批量"标记

### 4. 最后一组特殊处理

#### 问题描述

当只剩下最后一组号码时，如果用户不满意，就无法再反选（因为至少要保留一组）。

#### 解决方案

系统提供两个选项：

**选项1: 重新生成**
```
重新生成一批新的号码
原来的最后一组不会被保存
```

**选项2: 保留但不保存**
```
关闭弹窗，不保存任何号码
返回主选号页面手动选择
```

#### 用户提示

```
提示: 这是最后一组号码了
如果对这组也不满意，您可以选择：
✓ 重新生成一批新的号码
✓ 取消并返回主选号页面
```

---

## 最佳实践

### 1. 合理设置生成组数

```
初次使用: 5组
- 不会感到压力
- 快速了解功能
- 适合小批量购买

批量购买: 10组
- 更多选择空间
- 提高中奖概率
- 节省操作时间
```

### 2. 选择合适的策略

```
追求平衡: 平衡奇偶
- 号码分布均匀
- 符合统计学规律

追求和值: 和值范围
- 控制在合理范围
- 基于历史数据

追求随机: 完全随机
- 真正的随机性
- 不受任何限制
```

### 3. 反选技巧

#### 技巧1: 先浏览，再反选

```
第一步: 快速浏览所有组
第二步: 标记明显不喜欢的组
第三步: 仔细比较剩余组
第四步: 再次反选不满意的
第五步: 保存最终剩下的
```

#### 技巧2: 设置排除标准

```
标准示例:
- 包含不喜欢的数字 → 反选
- 和值过高或过低 → 反选
- 连号太多 → 反选
- 奇偶比例失调 → 反选
```

#### 技巧3: 保留多样化

```
建议保留:
- 不同的和值范围
- 不同的奇偶比例
- 不同的号码分布

避免保留:
- 过于相似的号码组
- 集中在某个范围
```

### 4. 批量保存后

```
保存后建议:
1. 查看历史记录
2. 为每组添加备注
3. 记录购买时的直觉感受
4. 开奖后对比结果
```

---

## 常见问题

### Q1: 反选和直接选有什么区别？

A:
- **直接选**: 手动一个个选择号码，适合有明确想法
- **反选**: 批量生成后排除不想要的，适合选择困难或想批量购买

### Q2: 为什么会有"最后一组"的限制？

A: 因为反选的目的是保留满意的号码。如果所有组都被反选了，就没有号码可保存了。所以至少要保留一组。

### Q3: 可以反选后重新生成吗？

A: 可以。您有两种方式：
1. 先保存当前剩余的，再重新生成新的一批
2. 直接关闭弹窗，重新开始

### Q4: 生成的号码会重复吗？

A: 系统会尽量确保生成的号码组各不相同，但不保证完全不重复（特别是在生成组数较多时）。

### Q5: 反选后可以恢复吗？

A: 可以。在点击"批量保存"之前，您可以随时点击"恢复"按钮取消反选。

### Q6: 批量保存的号码可以在哪里查看？

A: 保存在"历史记录"页面，每组号码都是独立的一条记录。

### Q7: 可以混合使用不同策略吗？

A: 当前版本一次只能选择一种策略。如需混合，可以：
- 第一次用"平衡奇偶"生成5组
- 第二次用"和值范围"生成5组
- 然后从10组中选择

### Q8: 反选功能是否支持AI推荐的号码？

A: 当前版本不支持。AI推荐和反选批量是两个独立功能。

### Q9: 最多可以生成多少组？

A: 建议5-10组，最多不超过20组。过多的组数会增加选择困难。

### Q10: 反选批量适合什么类型的用户？

A: 适合：
- 选择困难症患者
- 想要多买几注的用户
- 相信"排除法"的用户
- 追求效率的用户

---

## 技巧和建议

### 技巧1: 分批次生成

```
不要一次生成太多:
第一次: 生成5组，反选不满意
第二次: 再生成5组，继续反选
第三次: 保存所有剩余的

优势:
- 不会感到压力
- 保持新鲜感
- 提高满意度
```

### 技巧2: 结合个人直觉

```
反选后，对剩余的号码:
1. 快速浏览
2. 注意第一感觉
3. 标记特别有感觉的组
4. 优先考虑标记的组
```

### 技巧3: 设置筛选标准

```
提前设定标准，提高效率:
- 和值范围: 80-120
- 必须包含的数字: [7, 18]
- 必须排除的数字: [4, 13]
- 奇偶比例: 3:3 或 4:2

反选时参考标准快速判断
```

### 技巧4: 记录和反思

```
养成记录习惯:
1. 记录每次反选的标准
2. 记录最终选择的号码
3. 开奖后对比结果
4. 总结经验教训
```

### 技巧5: 灵活运用

```
不要拘泥于一种方式:
- 有明确想法: 直接手动选号
- 想要参考: 使用AI推荐
- 批量购买: 使用反选批量
- 综合考虑: 混合使用多种方法
```

---

## 功能对比

| 功能 | 反选批量 | AI推荐 | 随机选号 | 手动选号 |
|------|---------|--------|---------|---------|
| **速度** | 快 | 中 | 快 | 慢 |
| **个性化** | 低 | 高 | 低 | 最高 |
| **号码组数** | 多(5-10) | 中(5) | 少(1) | 自定义 |
| **理由说明** | 无 | 详细 | 无 | 个人 |
| **适用场景** | 批量购买 | 参考 | 快速选号 | 精确控制 |
| **需要API** | 否 | 是 | 否 | 否 |

**推荐使用顺序**:
1. 先用 **AI推荐** 获取灵感和分析
2. 再用 **反选批量** 生成多组号码
3. 结合 **手动选号** 微调不满意的地方
4. 最后使用 **随机选号** 作为补充

---

## 实际使用案例

### 案例1: 新手用户

```
用户: 小王（首次使用）

操作流程:
1. 打开反选批量
2. 选择生成 5 组
3. 选择"平衡奇偶"策略
4. 浏览生成的号码
5. 反选了第2、4组（不喜欢）
6. 保存剩余 3 组
7. 购买了这 3 组号码

结果: 快速获得3组满意的号码，节省时间
```

### 案例2: 批量购买

```
用户: 李先生（想买10注）

操作流程:
1. 第一次生成 10 组
2. 反选了 3 组
3. 保存剩余 7 组
4. 感觉不够，再生成 5 组
5. 反选了 2 组
6. 保存剩余 3 组
7. 总共保存 10 组

结果: 满足批量购买需求，号码经过筛选
```

### 案例3: 选择困难症

```
用户: 小张（选择困难）

操作流程:
1. 生成 10 组号码
2. 快速浏览，发现不喜欢有4的
3. 反选所有包含4的组（3组）
4. 再浏览，发现不喜欢和值太小的
5. 再反选和值小于90的（2组）
6. 剩余 5 组，感觉都不错
7. 随机选了其中 2 组购买

结果: 通过排除法，快速缩小范围
```

---

## 更新日志

### v1.1.0 (2026-02-08)
- ✨ 新增反选批量选号功能
- ✨ 支持生成5-10组号码
- ✨ 智能反选排除功能
- ✨ 批量保存剩余号码
- ✨ 最后一组特殊处理

---

## 相关功能

- [AI推荐](./ai-recommendations.md) - 智能推荐号码
- [随机选号](../../README.md#智能功能) - 快速随机生成
- [历史记录](../../README.md#历史记录) - 查看保存的号码

---

## 技术细节

### 数据结构

```typescript
interface ReverseSelectionState {
  totalGroups: number;        // 总组数
  groups: NumberGroup[];      // 号码组
  reversedIndices: number[];  // 已反选的组索引
  selectedGroups: NumberGroup[]; // 剩余的组
}

interface NumberGroup {
  id: string;
  redBalls: number[];
  blueBall: number;
  strategy: string;
  timestamp: number;
}
```

### 核心算法

```typescript
// 反选逻辑
function reverseSelection(
  groups: NumberGroup[],
  indexToRemove: number
): NumberGroup[] {
  // 移除指定索引的组
  return groups.filter((_, i) => i !== indexToRemove);
}

// 批量保存
function batchSave(
  groups: NumberGroup[]
): void {
  groups.forEach(group => {
    saveToHistory({
      ...group,
      source: 'reverse-selection',
      savedAt: Date.now()
    });
  });
}

// 最后一组检测
function isLastGroup(
  total: number,
  reversed: number
): boolean {
  return total - reversed === 1;
}
```

祝您选号顺利！🎯💰
